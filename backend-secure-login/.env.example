# Database
DATABASE_URL="postgresql://postgres:admin123@localhost:5432/secure_login_db?schema=public"

# JWT Secrets (cambia estos valores en producción)
JWT_ACCESS_SECRET="super-secreto-access-token"
JWT_REFRESH_SECRET="super-secreto-refresh-token"

# JWT Expiration
JWT_ACCESS_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# App Config
PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:5173/"

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10


# ======================
# WEBAUTHN CONFIGURATION
# ======================
RP_NAME="Secure Login App"           # Nombre de la aplicación
RP_ID="localhost"                     # Dominio (sin http://)
ORIGIN="http://localhost:5173/"       # URL completa del frontend

# FACIAL RECOGNITION
# ======================
# Clave para encriptar descriptores faciales (mínimo 32 caracteres)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FACE_ENCRYPTION_KEY="super-encryptatation-key-1234567890abcdef"